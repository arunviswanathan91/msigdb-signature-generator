# MSigDB Signature Generator - Requirements
# ==========================================
# PINNED to the versions from the last known-good Streamlit Cloud deploy.
#
# Why pinned: these were previously all floating (>=), so Cloud resolved
# whatever was newest at deploy time. That pulled pyarrow 25.0.1, which
# Streamlit had to auto-downgrade at runtime for a known segfault
# (apache/arrow#50471). Floating deps mean a deploy can break without a
# single line of code changing. Bump these deliberately, not by accident.

# Core Streamlit
streamlit==1.62.0
pandas==3.0.5
numpy==2.5.2
pyarrow==24.0.0          # pinned <25: apache/arrow#50471 segfault

# Machine Learning & Embeddings
sentence-transformers==6.0.0
transformers==5.15.1
scikit-learn==1.9.0

# torch is a hard runtime requirement via sentence-transformers but was never
# declared. On CPU-only hosts the default wheel drags in the full CUDA stack
# (~2.5 GB). To slim the deploy, swap the two lines below for:
#   --extra-index-url https://download.pytorch.org/whl/cpu
#   torch==2.13.0+cpu
# (verify that wheel exists for your Python version before switching)
torch==2.13.0

# LLM (Groq via the OpenAI-compatible SDK - see model_registry.py)
openai==3.3.1
huggingface-hub==1.28.0

# Data Processing
scipy==1.18.1

# Utilities
python-dateutil==2.9.0.post0
packaging==26.3
tqdm==4.70.0
requests==2.34.2
nest_asyncio==1.6.0

# Visualisation (imported at module scope in app.py - not optional)
plotly==6.9.0

# REMOVED: faiss-cpu
# Declared since v3.0 but never imported anywhere in the repo. Pathway
# similarity is a numpy cosine loop (app.py, cache_client.py), not FAISS.
# Dropping it removes a large wheel from every install.
